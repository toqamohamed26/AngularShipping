import { SearchidintityPipe } from './searchidintity.pipe';

describe('SearchidintityPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchidintityPipe();
    expect(pipe).toBeTruthy();
  });
});
